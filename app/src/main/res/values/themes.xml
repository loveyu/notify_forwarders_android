<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 使用正确的Material 3主题引用 -->
    <style name="Theme.NotifyForwarders" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 系统状态栏 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- 确保全屏体验 -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- 剪贴板浮动Activity主题 -->
    <style name="Theme.ClipboardFloating" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:excludeFromRecents">true</item>
        <item name="android:finishOnTaskLaunch">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>