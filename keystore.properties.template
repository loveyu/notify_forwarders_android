# Keystore配置文件模板
# 复制此文件为 keystore.properties 并填入实际值
# 注意：keystore.properties 文件应该被添加到 .gitignore 中，不要提交到版本控制

# Keystore文件路径（相对于app目录）
STORE_FILE=your-release-key.keystore

# Keystore密码
STORE_PASSWORD=your_keystore_password

# Key别名
KEY_ALIAS=your_key_alias

# Key密码
KEY_PASSWORD=your_key_password
